{{- if .Values.route.enabled }}
1. Get the application URL by running these commands:
  {{- if .Values.route.tls.enabled }}
  echo "https://{{ .Values.route.host }}"
  {{- else }}
  echo "http://{{ .Values.route.host }}"
  {{- end }}
{{- else }}
1. Get the application URL by running these commands:
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "agile-retro.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace {{ .Release.Namespace }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  echo "Visit http://127.0.0.1:8080 to use your application"
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:$CONTAINER_PORT
{{- end }}

2. Database Information:
  - The application uses PostgreSQL (schema auto-created on first connection).
  {{- if .Values.postgresql.enabled }}
  - Bundled Postgres: deployment/{{ include "agile-retro.fullname" . }}-postgres
    (persistence {{ if .Values.postgresql.persistence.enabled }}enabled, {{ .Values.postgresql.persistence.size }}{{ else }}DISABLED — data lost on restart{{ end }})
  - DATABASE_URL is injected from secret {{ include "agile-retro.fullname" . }}-postgres
  {{- else }}
  - External database: {{ if .Values.externalDatabase.existingSecret }}from secret {{ .Values.externalDatabase.existingSecret }}{{ else if .Values.externalDatabase.url }}configured via externalDatabase.url{{ else }}NOT SET — set externalDatabase.url or externalDatabase.existingSecret!{{ end }}
  {{- end }}

3. Authentication:
  {{- if .Values.auth.google.enabled }}
  - Google OAuth is enabled
  {{- end }}
  {{- if .Values.auth.keycloak.enabled }}
  - Keycloak OAuth is enabled
  {{- end }}
  {{- if not (or .Values.auth.google.enabled .Values.auth.keycloak.enabled) }}
  - Configure authentication by setting auth.google or auth.keycloak values
  {{- end }}

4. Environment Configuration:
  - AUTH_SECRET is {{ if .Values.auth.secret }}configured{{ else }}NOT SET - Please configure!{{ end }}
  - AUTH_URL: {{ if .Values.app.url }}{{ .Values.app.url }}{{ else }}NOT SET - inferred from request host (set app.url for OAuth behind a proxy){{ end }}
  - AUTH_TRUST_HOST: {{ .Values.app.trustHost }}

5. Useful Commands:
  # View logs
  kubectl logs -f deployment/{{ include "agile-retro.fullname" . }} -n {{ .Release.Namespace }}
  
  # Access the pod shell
  kubectl exec -it deployment/{{ include "agile-retro.fullname" . }} -n {{ .Release.Namespace }} -- /bin/sh
  {{- if .Values.postgresql.enabled }}

  # Open a psql shell on the bundled Postgres
  kubectl exec -it deployment/{{ include "agile-retro.fullname" . }}-postgres -n {{ .Release.Namespace }} -- psql -U {{ .Values.postgresql.auth.username }} -d {{ .Values.postgresql.auth.database }}
  {{- end }}

For more information, visit: https://github.com/opentooling/agile-retro
